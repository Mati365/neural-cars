/**
 * Do not use FP stuff here too!
 *
 * @param {Number[]}  preferredOutput
 * @param {Network}   network
 *
 * @returns {Network}
 */
const backwardPropagation = (preferredOutput, network) => {
  return network;
};

export default backwardPropagation;
